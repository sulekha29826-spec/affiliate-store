# Affiliate Store

Modern affiliate e-commerce store built with React + Node.js + PostgreSQL.

## Structure
- `frontend/` — React + Vite app (Cloudflare Pages)
- `backend/` — Node.js + Express API (Railway)

## Quick Start
See `backend/README.md` and `frontend/README.md`
